/**
 * Dirección base del backend (Spring Boot). En desarrollo corre en
 * localhost:8080; si el backend se despliega en otro lado, este es el
 * único lugar que hay que tocar.
 */
export const API_BASE_URL = 'http://localhost:8080';
